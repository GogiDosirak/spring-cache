services:
  redis:
    image: redis
    restart: always
    container_name: redis7
    ports:
      - "6379:6379"
    command: redis-server --port 6379
    volumes:
      - ./db/redis/data:/data
      - ./db/redis/conf:/usr/local/etc/redis/redis.conf